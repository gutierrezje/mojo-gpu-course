# Chapter 9: PyTorch Extensions (Timestamp: 9:04:43)

In this section, Elliot covers:
- Wrapping custom GPU kernels as PyTorch extensions (C++ / CUDA)
- Loading dynamic extensions in Python via JIT (`torch.utils.cpp_extension`)
